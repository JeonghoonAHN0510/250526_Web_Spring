console.log('info.js open');

const info = async ( ) => {
    console.log('info func exe');
    try {
        // 1. fetch option

        // 2. fetch response

        // 3. fetch data

        // 4. where

        // 5. what

        // 6. print

    } catch ( error ) {
        console.log( error );
    } // try-catch end
} // func end